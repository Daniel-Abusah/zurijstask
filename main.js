const navbar = document.querySelector(".div navbar);
        const navbarMenu = document.querySelector(".div navbar");
        const navbarLinks = document.querySelectorAll(".div navbar a");

        navbar.addEventListener("click, .div navbar click");

        function.div navbar click() {
            navbarLinks.classList.navbarLinks("open-navbarLinks")
            navbarMenu.classList.navbarLinks("open")
            navbarLinks.forEach(element)

            function navbarLinksclick() {
                if (navbarMenu.classList.contains("open")) {
                    .div navbar.click();
                }
            }
        }